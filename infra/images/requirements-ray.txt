# Python deps baked into the Ray worker image (<DOCKER_USER>/recsys-ray:v1).
# Pinned to match pyproject.toml `ml` group + base deps the 007 training code
# imports. Ray itself comes from the base image (rayproject/ray:2.44.1-py311-cpu)
# and already includes the tune/train extras, so it is NOT reinstalled here.
torch==2.6.0
lightning==2.5.1
mlflow==2.16.2
evidently==0.6.7
psutil>=5.9,<7
pandas>=2.1.4,<3.0.0
numpy==1.26.4
pyarrow==16.1.0
scikit-learn>=1.5.1,<2.0.0
pyyaml>=6.0,<7.0
dill>=0.3.8,<1.0.0
loguru>=0.7.2,<1.0.0
pydantic>=2.9.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
boto3>=1.35.37,<2.0.0
hyperopt==0.2.7