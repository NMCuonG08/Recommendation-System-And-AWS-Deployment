apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qdrant.fullname" . }}
  labels:
    {{- include "qdrant.labels" . | nindent 4 }}
data:
  production.yaml: |
    {{- tpl (toYaml .Values.config) . | nindent 4 }}
