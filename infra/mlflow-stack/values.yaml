# MLflow stack values (in-cluster Postgres + MinIO, mirroring docker-compose).
namespace: mlflow

mlflow:
  # Built from infra/images/mlflow.Dockerfile; push via infra/scripts/build_push.sh.
  image: nmcuong08/recsys-mlflow:v1
  s3Endpoint: http://minio-service:9000
  bucket: mlflow-artifacts

minio:
  accessKey: admin
  secretKey: Password1234
  storage: 5Gi
  image: quay.io/minio/minio:RELEASE.2024-10-13T13-34-50Z

postgres:
  password: Password1234
  storage: 2Gi
  image: postgres:16

storageClass:
  name: gp3
  provisioner: ebs.csi.aws.com
  type: gp3
  fsType: ext4