# Python cache & build artifacts
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Jupyter Notebook checkpoints
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Virtual environments
.venv/
venv/
ENV/
env/

# Environment variables and secrets
.env
.env.*
!.env.example

# Lock files (Optional: commit if this is a deployed application, ignore if library)
uv.lock

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
.project
.pydevproject
.settings/

# OS-specific files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# Machine Learning: Data and Model weights (Avoid committing large files to Git)
data/
**/data/
*.csv
*.parquet
*.pkl
*.pickle
*.h5
*.onnx
*.pt
*.pth
*.joblib
*.npy
*.npz
*.ckpt
*.tfevents.*

# MLflow & Airflow (Based on pyproject.toml dependencies)
mlruns/
mlruns.db
mlflow.db
logs/
ray_results/
airflow.db
airflow.cfg
webserver_config.py

# Feast (Feature Store) local registry cache
.feast/

# Terraform (EKS infra — never commit state or tfvars with secrets)
infra/**/terraform.tfstate
infra/**/terraform.tfstate.*
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/*.tfvars
infra/**/*.tfvars.json
infra/**/.terraform/
infra/**/.terraform.lock.hcl
infra/**/*.tfplan

# Generated feature-engineering + model outputs (regenerated by notebooks
# 004-006; baked into the ray image at docker-build time, not committed).
# Keep dir placeholders (.gitkeep) but ignore regenerated data.
feature/output/**/*.jsonl
feature/output/**/*.json
feature/output/**/*.dill
feature/output/**/*.parquet
feature/feature_store/registry.db
models/output/**/*.ckpt
models/output/**/*.pt
models/output/**/*.pth
models/output/**/*.dill
models/output/**/*.pkl
models/output/**/*.onnx
# Regenerable run artifacts (Evidently HTML, id mapper, HP search results, logs, trash).
# Keep committed: reports/figures/*.png + reports/*.json (referenced by docs).
models/output/**/evidently_report_*.html
models/output/**/id_mapper.json
models/output/**/idm.json
models/output/**/hp_search_results.json
models/output/**/*.log
models/output/**/.trash_*/

# Triton model repository (ONNX + dill + configs) — regenerated by
# `models/ranking_sequence/convert2onnx_and_build_triton.py`, not committed.
models/ranking_sequence/model_repository/